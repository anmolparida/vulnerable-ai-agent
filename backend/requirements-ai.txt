# ============================================================================
# AI/ML DEPENDENCY MANIFEST — SCAN-ONLY, DELIBERATELY VULNERABLE
# ----------------------------------------------------------------------------
# Every line below is pinned to a version with at least one PUBLIC CVE so that
# software-composition / AI-package scanning (e.g. Qualys TotalAI) raises a
# finding. This file is a *manifest for scanners* — it is intentionally NOT
# installed by the Dockerfile (torch/tensorflow/etc. would bloat the image and
# aren't needed to run the mock agent). Install at your own risk in a lab only:
#     pip install -r requirements-ai.txt
# All versions are real PyPI releases. See CHECKS.md for the full CVE map.
# ============================================================================

# --- LLM / agent frameworks -------------------------------------------------
langchain==0.0.235               # CVE-2023-36258 & CVE-2023-29374 arbitrary code exec (LLMMathChain/PALChain); fixed 0.0.236+
langchain-experimental==0.0.44   # CVE-2024-46946 code exec via LLMSymbolicMathChain (numexpr); fixed 0.3.0
llama-index==0.9.30              # CVE-2023-39662 arbitrary code exec (PandasQueryEngine); CVE-2024-3271 command injection
transformers==4.35.0             # CVE-2023-6730 RCE via malicious model config / trust_remote_code path; fixed 4.36.0
vllm==0.5.0                      # multiple 2024 advisories: unsafe deserialization + DoS

# --- deep-learning runtimes / model formats --------------------------------
torch==2.5.0                     # torch.load weights_only RCE bypass (GHSA-53q9-r3pm-6pq6, fixed 2.6.0); CVE-2024-48063 RemoteModule deser RCE
tensorflow==2.11.0               # dozens of CVEs (heap/OOB, integer overflow) fixed across 2.12/2.13
keras==2.12.0                    # CVE-2024-3660 arbitrary code exec via Lambda layer on model load; safe_mode added 2.13
onnx==1.15.0                     # CVE-2024-27318 directory traversal on external_data load; fixed 1.16.0
ray==2.6.3                       # CVE-2023-48022 "ShadowRay" unauthenticated RCE via Jobs API (default no-auth)
mlflow==2.9.2                    # CVE-2024-37052..37060 pickle-deserialization RCE + CVE-2023-6018/6014 path traversal/LFI

# --- data / feature libraries commonly in AI stacks -------------------------
pyarrow==14.0.0                  # CVE-2023-47248 arbitrary code exec loading a crafted IPC/Parquet file; fixed 14.0.1
scikit-learn==1.4.0              # CVE-2024-5206 sensitive-data leak via TfidfVectorizer stop_words_; fixed 1.5.0
nltk==3.8.1                      # CVE-2024-39705 RCE via untrusted pickle in downloaded packages; fixed 3.9
pillow==10.0.0                   # CVE-2023-50447 arbitrary code exec via ImageMath.eval; CVE-2023-4863 WebP heap overflow
gradio==4.10.0                   # CVE-2023-51449 file traversal / SSRF via /file endpoint; fixed 4.11.0

# --- serving / infra pulled into AI apps ------------------------------------
aiohttp==3.9.1                   # CVE-2024-23334 path traversal via static routes with follow_symlinks; fixed 3.9.2
gunicorn==21.2.0                 # CVE-2024-1135 HTTP request smuggling via Transfer-Encoding; fixed 22.0.0
cryptography==41.0.0             # CVE-2023-50782 / CVE-2024-26130 NULL-deref & Bleichenbacher timing; fixed 42.x
setuptools==65.5.0               # CVE-2024-6345 RCE via package_index download; CVE-2022-40897 ReDoS
