# NOTE (intentional weakness LLM02 / SAST): .env is deliberately NOT ignored here,
# so committed secrets end up in version control. Do not "fix" this.
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
# .env        <-- intentionally left commented out so secrets get committed

# macOS + Cowork artifacts (not part of the target)
.DS_Store
Claude outputs/
